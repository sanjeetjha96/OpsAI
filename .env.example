# Example .env (do NOT commit real secrets). Copy to `.env` and fill values.

OPENAI_API_KEY=sk-REPLACE_ME
OPENAI_EMBED_MODEL=text-embedding-3-small
GOOGLE_ADK_CONFIG=/path/to/google/adk/config.json
GOOGLE_ADK_PROJECT=my-gcp-project
RAG_INDEX_PATH=data/index.json
MEMORY_DB=memory/memory.db
